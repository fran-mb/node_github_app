const isValid = true;

// if/else

if (!isValid) {
  console.log("valid");
} else {
  console.log("invalid");
}

// ternary operator     condition ? mets : otherwise

isValid ? console.log("valid") : console.log("invalid");
